# Load all the appropriate variables
- debug:
    msg: 'Load variables from {{ role_path }}'

#- include_tasks: ../../../../tasks/util/load-vars-hierarchy.yml
#  vars:
#    hierarchy_dir: '{{ role_path }}'


- import_tasks: cloud-email-vpc.yml
  tags:
    - install
    - uninstall

- import_tasks: s3-buckets.yml
  tags:
    - install
    - uninstall

- import_tasks: security-groups.yml
  tags:
    - install
    - uninstall

- import_tasks: iam-roles.yml
  tags:
    - install
    - uninstall

- import_tasks: sqs-queues.yml
  tags:
    - install
    - uninstall

- import_tasks: sns-topics.yml
  tags:
    - install
    - uninstall

- import_tasks: xgemail-messaging.yml
  tags:
    - install
    - uninstall

- import_tasks: bastion.yml
  tags:
    - install
    - uninstall


































