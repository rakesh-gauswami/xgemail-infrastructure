Miscellaneous tools.

check_json:
    Run this to detect JSON formatting errors.

vpc_generate_property_files:
    Run this on a classic mastercc instance to generate the application,
    bootstrap, and service.properties files for each P0 and UPE Java
    server type in each region.  These files can then be uploaded to
    a hopper instance and then to s3.
