<filter sophos.xgemail.<%= @application_name %>.maillog>
  @type parser
  key_name message
  reserve_data false
  reserve_time true
  <parse>
    @type grok
    custom_pattern_path <%= @patterns_dir %>/postfix
    grok_pattern %{PF}
    grok_failure_key grokfailure
  </parse>
</filter>
