<filter sophos.xgemail.<%= @application_name %>.jilter>
  @type parser
  key_name message
  reserve_data true
  reserve_time true
  <parse>
    @type grok
    custom_pattern_path <%= @patterns_dir %>/jilter
    grok_pattern %{JILTERLOG}
    grok_failure_key grokfailure
  </parse>
</filter>
