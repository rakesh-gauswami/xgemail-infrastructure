<source>
  @type tail
  format /^(?<time>[\w]+\s+[\d]+\s[\d:]+)\s+(?<message>.*)/
  time_format %b %d %H:%M:%S
  path /var/log/maillog
  pos_file /var/log/td-agent/maillog.pos
  tag raw.postfix.maillog
</source>