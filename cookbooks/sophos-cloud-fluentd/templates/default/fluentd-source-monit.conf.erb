<source>
  @type tail
  format /^(?<time>[^ ]+) (?<host>[!-~]{1,255}) (?<process>[^:]+): (?<message>.*)/
  time_format %b %d %H:%M:%S.%N
  path /var/log/monit
  tag sophos.xgemail.<%= @application_name %>.monit
  pos_file /var/log/td-agent/monit.pos
</source>
