<match match sophos.xgemail.**>
  @type kinesis_firehose
  region <%= @region %>
  delivery_stream_name <%= @delivery_stream_name %>
  <format>
    @type json
  </format>
  <inject>
    time_key time
    tag_key tag
  </inject>
</match>
