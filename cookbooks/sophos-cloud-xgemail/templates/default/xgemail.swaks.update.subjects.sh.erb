#!/bin/bash

OLDSUB=$(grep -o '".*"' <%= @swaks_dir %>/send_warmup_emails.sh |awk -F'--' '{ print $1 }'|uniq )
NEWSUB=$(shuf -n 1 <%= @swaks_dir %>/subject_lists.txt)
eval $(echo "sed -i 's/$OLDSUB/$NEWSUB /g' <%= @swaks_dir %>/send_warmup_emails.sh")
